import styled from 'styled-components';

const Wrapper = styled.div`
  h3 {
    font-size: 1.5rem;
    text-align: center;
  }
  p {
    line-height: 2;
    color: var(--dark);
    margin-top: 2rem;
  }
`;

export default Wrapper;
